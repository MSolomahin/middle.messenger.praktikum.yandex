import { ComponentBaseProps } from '../../core/component/component.types'
import Validator from '../../utils/validator'

export interface ChangePasswordFormProps extends ComponentBaseProps {
    validator: Validator
}

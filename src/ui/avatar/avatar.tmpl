<div class="{% isEditable && avatar_editable %}">
  <label class="avatar__hover-block hover-block">
    <input
      class="hover-block__input"
      type="file"
      accept="image/*"
      name="avatar"
    />
    <p class="text-m text-white hover-block__text">Change avatar</p>
  </label>
</div>
{% src && <img src="{{ src }}" class="avatar__img"/> %}

<input
  type="{{ type }}"
  required
  name="{{ name }}"
  required="{{ required }}"
  value='{{ value }}'
/>
<label>{{ label }}</label>
{% isError && <p class="input-base__error text-xs">{{ errorMessage }}</p> %}
